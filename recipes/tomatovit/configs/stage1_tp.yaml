defaults:
  - stage1

# Tensor-parallel example config.
# Adjust mesh sizes so dp_size * fsdp2_size * tp_size == WORLD_SIZE.
parallel:
  type: "fsdp2"
  mesh:
    dp_size: 1
    fsdp2_size: 1
    tp_size: 2