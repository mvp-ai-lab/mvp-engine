defaults:
  - stage1

# FSDP2 + Tensor-parallel example config.
# Adjust mesh sizes so dp_size * fsdp2_size * tp_size == WORLD_SIZE.
parallel:
  type: "fsdp2"
  mesh:
    dp_size: 1
    fsdp2_size: 2
    tp_size: 2
  backend_kwargs:
    reshard_after_forward: true
    mp_policy:
      param_dtype: bfloat16
      reduce_dtype: float32
      output_dtype: bfloat16
    high_precision_modules: ["LayerNorm", "RMSNorm"]
