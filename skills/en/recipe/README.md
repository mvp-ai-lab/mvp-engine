# Recipe

Skills for **new experiment / recipe setup** (e.g. scaffolding a new recipe folder, wiring engine + config + dataset). The layout is standard but each recipe has different config and entry points.

No skills in this category yet.
