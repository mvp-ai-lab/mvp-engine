# Debug

Skills for **debugging and performance analysis** (e.g. memory profiling, loss debugging, finding OOM causes). The workflow is pattern-based but depends on the failure mode and model.

## Skills

- `recipe-merge-repair`: inspect merged shared-code changes, determine whether they broke the current recipe, and repair recipe-local code/configs before stopping.
