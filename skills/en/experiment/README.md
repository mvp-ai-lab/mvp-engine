# Experiment

Skills for **experiment result analysis and reporting**. Use these when the task is not adding a new recipe, but turning existing run artifacts into a structured conclusion, comparison, or report.

## Skills

- `analysis`: turn `mvp-engine` run outputs such as `config_<run_id>.yaml`, `log_<run_id>.log`, `checkpoints/`, and optional external eval artifacts into a structured experiment report.
