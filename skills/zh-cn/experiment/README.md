# Experiment

与 **实验结果分析和报告撰写** 相关的 skill。适用于已经有运行产物，需要把结论、对比和问题诊断整理成结构化报告，而不是继续搭新的 recipe。

## Skills

- `analysis`: 把 `mvp-engine` 的运行产物（如 `config_<run_id>.yaml`、`log_<run_id>.log`、`checkpoints/`）以及可选的外部评测结果整理成结构化实验报告。
