# Data

Skills for **data pipeline integration** (e.g. adding a new dataset type, custom augmentation, webdataset layout). Patterns are reusable but each data source has its own format and wiring.

No skills in this category yet.
