# The End

You now know the three main surfaces of MVP-Engine:

- `Engine` for runtime flow
- `Recipe` for experiment code
- `Skill` for agent-guided extensions

These pieces are meant to stay small, direct, and practical.
