# MVP-Engine Tutorial

空白 Vite + React + TypeScript + Tailwind CSS + Framer Motion 项目骨架。

## Run

```bash
pnpm install
pnpm dev
```

Then open the Vite URL in a browser.

## Build

```bash
pnpm build
```
